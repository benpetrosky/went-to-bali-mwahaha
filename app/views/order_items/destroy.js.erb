$("#<%= @item.id %>").remove();
$('#cart_items_nav').html('<%= link_to "Cart | Total items: #{ cart_items }", cart_path %>');
$('.runningTotal').html('<%= number_to_currency current_order.total_price %>');
